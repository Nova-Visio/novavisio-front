/** Ruta del módulo auth. */
export const auth = 'auth';

/** Ruta del módulo customers. */
export const customers = 'clientes';

/** Ruta del módulo customers list. */
export const customersList = '';

/** Ruta de dashboard. */
export const dashboard = 'dashboard';

/** Ruta de login. */
export const login = 'login';

/** Ruta de notifications. */
export const notifications = 'notificaciones';

/** Ruta de notifications list. */
export const notificationsList = '';

/** Ruta de orders. */
export const orders = 'compras';

/** Ruta de orders list. */
export const ordersList = '';

/** Ruta de products. */
export const products = 'productos';

/** Ruta de products list. */
export const productsList = '';

/** Ruta de products form. */
export const productsForm = 'nuevo';
